# Voice pipeline
openai-whisper>=20231117
elevenlabs>=1.0.0
anthropic>=0.40.0
openai>=1.55.0                # client LLM via OpenRouter (compatible OpenAI Chat Completions)
edge-tts>=6.1.10              # TTS gratuit Microsoft Neural (voix Andrew)

# HTTP server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9       # FastAPI UploadFile (route /intent/audio)

# HTTP client (TaHoma, Freebox, Devialet)
requests>=2.32.0
httpx>=0.27.0

# Device clients
devialet>=1.5.7
# Freebox : appels HTTP direct via requests (pas de lib PyPI fiable côté sync)
# Freebox TV — play_youtube : recherche vidéo (yt-dlp) + cast YouTube Lounge (pyytlounge)
yt-dlp>=2024.0.0
pyytlounge>=3.0.0

# Google APIs
google-api-python-client>=2.150.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Vision (optionnel, S2)
opencv-python>=4.10.0
ultralytics>=8.3.0
fast-alpr>=0.1.0
watchdog>=5.0.0

# Config & secrets
python-dotenv>=1.0.0
pyyaml>=6.0.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Logging
rich>=13.0.0
